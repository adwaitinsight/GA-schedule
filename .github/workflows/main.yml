name: scheduling a workflow
on:
  schedule:
    - cron: '20 07 07 08 03' 
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run a script
        run: echo "This is a Scheduled Action"
